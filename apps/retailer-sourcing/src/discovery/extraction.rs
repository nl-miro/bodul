#![allow(dead_code)]
//! Extractors — the per-page logic that turns a fetched response into product
//! data. Stubs for now; see docs/sourcing-flow.md.

/// Extract a single product's data from its fetched details page.
pub fn extract_product_details_data() {
    todo!("extract product details data")
}

/// Split a fetched category/listing page into its individual products.
pub fn slice_products() {
    todo!("slice products from a category listing page")
}

/// Extract product data available on a category listing page.
pub fn extract_category_product_data() {
    todo!("extract category product data")
}
